<style lang="less" scoped>
.replyer-avatar {
  padding: 4px;
  border: 1px solid #ddd;
  border-radius: 4px;
}
.reply-diff {
  background-color: #d8d8d8;
}

</style>
<template>
  <div class="weui-panel weui-panel_access">
    <div class="weui-panel__bd">
      <div v-for="log in withdrawLogs" :key="log.id">
        <div class="weui-media-box weui-media-box_appmsg" hover-class="weui-cell_active">
          <a class="weui-media-box__hd weui-media-box__hd_in-appmsg" url="/pages/users/show?id={{ log.user_id }}">
            <image class="replyer-avatar weui-media-box__thumb" :src="log.user.avatar" />
          </a>
          <div class="weui-media-box__bd weui-media-box__bd_in-appmsg">
            <div class="weui-media-box__title">{{ log.user.name }}</div>
            <div class="weui-media-box__desc"><rich-text nodes="{{ log.id }}" bindtap="tap"></rich-text></div>
            <div class="weui-media-box__desc"><datetime-diff :datetime="log.created_at"></datetime-diff></div>
          </div>
        </div>
      </div>
      <div class="weui-loadmore weui-loadmore_line" v-if="noMoreData">
        <div class="weui-loadmore__tips weui-loadmore__tips_in-line">没有更多数据</div>
      </div>
    </div>
  </div>
</template>
<config>
  {
    usingComponents: {
      "datetime-diff": "~@/components/datetime-diff",
    }
  }
</config>
<script>
  import wepy from '@wepy/core'

  wepy.component({
    options: {
      styleIsolation: 'apply-shared'
    },
    props: {
      // 父页面传入，请求参数
      withdrawLogs: Array,
      noMoreData: Boolean
    }
  })
</script>
